#!/bin/bash

# This is an example of a webhook
echo "Hello World!"

# Output is logged via servepage's logs so look for "Hello World!" there.
# Normally you'd do something like "git pull origin master"
